services:
  ai-api:
    build:
      context: .
      dockerfile: Dockerfile.dev
    environment:
    - ENVIRONMENT=development
    - DEBUG=true
    ports:
    - 8000:8000
    volumes:
    - .:/app
version: '3.8'
